{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for GameLog (CT-16, CT-17)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report ../playwright-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
